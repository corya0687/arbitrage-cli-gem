require 'spec_helper'

describe Arbitrage do
  it 'has a version number' do
    expect(Arbitrage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
